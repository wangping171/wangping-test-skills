interface:
  display_name: "Text Length Checker"
  short_description: "Count text with clear Unicode rules"
  default_prompt: "Use $text-length-checker to count this text and check it against a 280-character limit."
